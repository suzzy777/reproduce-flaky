Currently Oozie restricts resolution of system properties by default. In some cases that would be nice if as an administrator I could set a whitelist of enabled system property names.

Suggestion will be provided asap.