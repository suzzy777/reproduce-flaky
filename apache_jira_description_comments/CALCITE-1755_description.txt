Tests in the Pig module have been flapping (e.g. CALCITE-1561) due to intermittent exception coming from running Pig locally. The tests need to be split up into unit and integration tests. Only integration tests should actually execute Pig.