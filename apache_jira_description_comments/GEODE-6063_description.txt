develop pipeline is expected to publish to snapshot repository on every successful build, however releases are signed and published manually, never by the pipeline, so PublishArtifacts job should not appear in release pipelines, only develop pipeline.