It would be nice to have a variant of mkdirs that provided an error message explaining why it failed.  This would make it easier to debug certain failing unit tests that rely on mkdir / mkdirs-- the ChecksumFilesystem tests, for example.