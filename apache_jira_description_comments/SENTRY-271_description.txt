Depending on the machine configuration the test {{TestSentryServiceIntegration}} can be always failing. The problem is that in our tests we are assuming that local hostname will always be a "localhost", however some boxes might have configured different one (such as {{localhost.localdomain}}.