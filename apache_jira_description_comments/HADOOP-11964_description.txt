When running determine-flaky-tests against precommit-hadoop-build, it throws a lot of errors because it assumes that every job is actually running Java tests.  There should be some way to make it not do that or at least fix its assumptions.