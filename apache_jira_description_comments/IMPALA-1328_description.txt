We have tests/experiments/test_process_failures.py to test impala's handling of process failures. The test test_restart_catalogd was failing, this was documented in IMPALA-1217. That JIRA was reopened after the test failed again, but now it only fails intermittently, indicating a flaky test.