The pull request is now merged.  Thanks, [~angerszhuuuu]!

Reopened due to it's still failing on master, for example:

[https://github.com/apache/ratis/actions/runs/4319922195/jobs/7539629473#step:5:581]

 

I'll submit another PR to fix this.

[~ckj], let's file a new JIRA to tack the new PR (unless we want to revert the old PR).

{quote} let's file a new JIRA to tack the new PR (unless we want to revert the old PR).
{quote}

[~szetszwo] OK, got it. Thanks for creating the new Jira.

