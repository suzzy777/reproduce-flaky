Currently the user can mention retry interval and maximum number of retries. We will add another element in action tag through which the user can mention policy for the retry. Policy could be exponential or periodic. Periodic will be the default as it is the current policy.