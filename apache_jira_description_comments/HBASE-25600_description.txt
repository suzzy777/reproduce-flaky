The flaky test list got horrible on branch-2.2 and the HBase-Flaky-Tests can't finish in the 2 hours timeout so the list keeps increasing.

Change the timeout on this branch to 4 hours so the job could finish.