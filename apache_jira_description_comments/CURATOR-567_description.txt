Calls to {{TestCleanState.closeAndTestClean()}} in the code have realized their purpose and can likely be removed. They add flakiness to our tests. I'll leave this ticket open and slowly remove calls from the code as Travis complains about them.