We need to make the camel-sjms tests run in parallel. To do so, we need to rework them to not use the same resources, as such, names of queues, topics, files and other non-shareable resources need to be adjusted. 