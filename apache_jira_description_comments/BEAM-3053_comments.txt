This issue is probably a duplicate of https://issues.apache.org/jira/browse/BEAM-3040 (timeout after 150 minutes).  Failed assert is expected, {{test_assert_that}} is testing that assert fires.

I see, thanks. In this case the test is working as expected. 

