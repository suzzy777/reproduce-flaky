At the moment SqlSchemaManager ignores write-only index status and returns all indices, which may lead to scan/key-lookups over an index that is not fully built.
Update SqlSchemaManager to ignore such indices. 