Automated unit test execution through the GitHub CI runner fails periodically on unit tests for InvokeHTTP and PutTCP where TLS is being used for socket communication.  These unit tests should be evaluated and modified to improve reliability and reduce the potential for timeouts.