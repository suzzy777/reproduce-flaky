Embed logic of selecting highest versioned region server for system table regions in AssignmentManager.processAssignQueue(). This way from any section of the code when system table regions are re/assigned, only highest versioned RS are candidates for target servers.