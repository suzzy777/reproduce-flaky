A typical developer does not want to run, for instance, {{rat}} or {{javadoc}} as part of their standard build workflow.  A {{devBuild}} target should only build classes and, likely, also include {{spotlessApply}}.