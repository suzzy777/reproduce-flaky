This is to add client capability checking to Hive metastore.
This could have been used, for example, when introducing ACID tables - a client trying to get_table on such a table without specifying that it is aware of ACID tables would get an error by default.