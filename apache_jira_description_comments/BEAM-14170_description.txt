When we sickbay tests we should continue to run them, at least in a new test suite that may hold flaky/sickbayed tests - often we just stop running them forever.

Create a new test for Python and Java that runs the current sickbayed tests.
