Both Java precommit and multiple Java postcommit suites have parallel builds disabled. This has an intensely bad effect on runtimes, to the point where it can even change our workflows. We need to get parallel builds back ASAP.