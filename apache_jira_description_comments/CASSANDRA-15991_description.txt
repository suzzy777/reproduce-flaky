As per CASSANDRA-15583 many in tree tools lack proper UX tooling: mandatory params are indeed mandatory, 'help' produces an actual help, return codes etc

This ticket is an attempt to add it to those tools that classify as LHF. Other tools such as nodetool, with many sub-commands, deserve a separate ticket of their own