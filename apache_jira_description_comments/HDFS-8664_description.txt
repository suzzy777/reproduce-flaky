We have many disks per machine (12+) that don't always have the same numbering when they come back from provisioning, but they're always in the same tree following the same pattern.

It would greatly reduce our config complexity to be able to specify a wildcard for all the data directories.