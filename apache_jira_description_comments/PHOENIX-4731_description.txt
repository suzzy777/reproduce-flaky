Different users may not be relying on transactions, or may only be relying on a single transaction provider. By default, we can run transactional tests across all providers, but we should have a way of disabling the running of a given provider.