Two interpreters are based same codes and JDBCInterpreter is more generic then HiveInterpreter. I think we just updated documentation to add hive dependencies in a interpreter tab and example setting.