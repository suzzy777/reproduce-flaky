Sentry-2283 introduced a way to make sentry start up by shading jars.  However, Intellij cannot handle the shaded jars as a transitive dependency.  Any time a dev uses intellij, shaded package names prevent a compile.  This does not affect the command line.