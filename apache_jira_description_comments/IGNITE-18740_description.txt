ItLearnersTest#testChangePeersToAddLearnerToSameNodeAsPeer has become flaky (it fails with a TimeoutException). This is mostly likely caused by a custom Raft retry timeout value in the configuration (which was changed to speed up some tests). This timeout should be changed back to the default value.