Currently when viewing different note revisions, it's still possible to edit the notes (e.g. ZEPPELIN-1190). First of all, notes should be view-only when viewing revisions. Secondly, note broadcasts shouldn't affect that mode, say `offline` mode. 