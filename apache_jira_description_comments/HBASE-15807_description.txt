It seems that testResult's output has changed and instead of marking failed tests' status as "REGRESSION" (like [here|https://builds.apache.org/view/All/job/HBase-Trunk_matrix/jdk=latest1.7,label=yahoo-not-h2/888/testReport/api/json]), it now marks them as "FAILED".
Updating the script. 