Track warnings currently is nested which is discouraged at the moment.  It also was before the config standards patch which moved storage typed longs to a new DataStorageSpec type, we should migrate the configs there.