Test fails sporadically on TC due to intensive disk usage. (Query1 and Queries (Binary) suites).

We should move this test to "PDS  with Indexing" suite.
Also, we have to check if GridIndexRebuildWithMvccEnabledSelfTest also runs on TC agents with SSD disks.

 