This can be used by AM while starting its webapp.
Currently a random port is chosen, which is never a good idea.
 
And {{Webapps}} has facility to allow only a specific port to be configured which may be occupied. A port range in case of AM, hence will be useful.