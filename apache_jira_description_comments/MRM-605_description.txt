there is one test failure in the repository-layer when built from the source distribution - though it remains fine on trunk. I'm not sure what's different.

It detects 6 results instead of 5 in a query.