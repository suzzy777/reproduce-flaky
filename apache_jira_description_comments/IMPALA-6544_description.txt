Every now and then, we hit a flaky test on S3 runs due to files missing when they should be present, and vice versa. We could consider running our tests (or a subset of our tests) with S3Guard to avoid these problems, however rare they are.