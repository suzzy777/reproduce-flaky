Currently, for a key, we create a block size byteBuffer in order for caching data. This can be replaced with an array of buffers of size flush buffer size configured for handling 2 node failures as well.