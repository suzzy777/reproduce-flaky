It would be good to be able to run the default clientpositive tests. Currently we can not do that, since we start with a specific database. We should filter the query input and replace the table references