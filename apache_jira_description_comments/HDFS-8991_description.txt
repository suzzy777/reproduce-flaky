In cases like HDFS-7714 where the BPOfferService thread is missing, which require nontrivial effort to debug that which NN that the DN thinks it active / standby.

It would make sense to make the information more accessible through JMX or Web UI.