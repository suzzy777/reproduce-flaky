`JavaKafkaStreamSuite.testKafkaStream` assumes when `sent.size == result.size`, the content of `sent` and `result` should be same. However, that's not true. The content of `result` may not be the final content.