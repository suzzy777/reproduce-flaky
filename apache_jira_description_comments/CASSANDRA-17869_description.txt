Add JDK17 option to cassandra-builds build-scripts, they only currently support options {{8}} and {{11}}.

Add JDK17 to the matrix axes in the jenkins dsl.

Ensure JDK17 is installed on all the jenkins agents.