When running map joins in llap, it can potentially use more memory for hash table loading (assuming other executors in the daemons have some memory to spare). This map join conversion decision has to be made during compilation that can provide some more room for LLAP. 