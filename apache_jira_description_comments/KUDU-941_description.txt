We should not delete DELETED tablets immediately. The current plan is to delete them on startup, and possibly with a background thread that polls once a minute or so. This requires some reworking of the delete table unit tests