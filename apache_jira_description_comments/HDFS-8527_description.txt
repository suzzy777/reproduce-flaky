This issue tracks development of OzoneHandler.  This is a component within the DataNode that receives inbound requests parsed from the REST interface, dispatches to the underlying storage container data pipeline, and then returns an appropriate response to the REST layer for translation to an outbound HTTP response.