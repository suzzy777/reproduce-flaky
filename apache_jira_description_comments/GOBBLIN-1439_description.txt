FlowSpecs that fail to compile can cause the Job Scheduler to silently fail adding jobs. Intended behaviour is to reject flows that fail compilation errors, and if encountering a flow that fails to compile, log it and continue scheduling the rest of the flows instead of silently failing