The initial MVP may only work in some restricted circumstances (e.g. @yields_element on process_batch, or batch-to-batch without a 1:1 input:output mapping might not be supported). These cases should fail early. 