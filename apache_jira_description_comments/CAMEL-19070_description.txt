It seems that ElasticSearch tests are flaky on CI. It could be that the CI nodes don't have enough resources to run the tests (needs to be investigated and then to ask infra if they can adjust the parameters).

 

Attached logs: [^camel-elasticsearch-test.log]