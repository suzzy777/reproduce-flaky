It reloads from the config because we offer users the ability to update the seed list without restarting the node. This is done by a user updating the yaml file and then the seed provider re-reads the file. 

ok, closing it then

