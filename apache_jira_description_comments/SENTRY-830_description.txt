Started to see many hive e2e test failures from real cluster test jobs, however when run tests locally, tests always pass. Enable setMetastoreListener so that the tests can have consistent results between local and real cluster setup.