Unit tests that require running the block deleting service in TestSchemaOneBackwardsCompatability sometimes fail. This is an issue with how the block deleting service is used within the tests, and is probably not indicative of an actual issue in the new code being run through CI.