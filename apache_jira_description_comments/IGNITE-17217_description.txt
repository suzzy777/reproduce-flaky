When all node connections are lost, *ClientAddressFinder.getAddresses* is not called to refresh the list of known endpoints.
For example, when a Kubernetes pod restarts, it may have a different address.