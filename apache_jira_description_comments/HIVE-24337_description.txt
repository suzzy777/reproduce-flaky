HIVE-23824 added the functionality of caching metadata part of orc files in LLAP cache, so that ACID reads can be faster. However the content itself still needs to be read in every single time. If this could be cached too, additional time could be saved.