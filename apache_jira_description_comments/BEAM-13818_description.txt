Currently, we only support private keys with passphrase in SnowflakeIO. We should also support unencrypted (without passphrase) private keys. This is helpful when the key is secured in some other way, e.g. it is provided by Google KMS, so there is no need to use an additional password.

 

Internal tracker: 