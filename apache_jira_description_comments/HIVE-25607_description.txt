The totalSize tbl prop can change whenever the file format version changes, therefore potentially causing the q tests to be flaky when issuing describe formatted commands. We should mask this and not test against the exact value.