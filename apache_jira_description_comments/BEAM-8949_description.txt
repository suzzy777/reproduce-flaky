Spanner IO (Python SDK) contains PTransform which uses the BatchAPI to read from the spanner. Currently, it only contains direct runner unit tests. In order to make this functionality available for the users, integration tests also need to be added.