As of May 8, 2022 the kotlin cross test suite only has `binary` protocol enabled, but we can further support `json`, `compact` protocols as well.

The support for multi, multij, and multic should also be good to have.