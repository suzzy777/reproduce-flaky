This causes hangs, see IGNITE-20750. We should use either get() (which is interruptible) or get(timeout) (which is better for the production use). When this component is being stopped, the network is already cut off, so some internal stuff not finished properly does not seem to be harmful.