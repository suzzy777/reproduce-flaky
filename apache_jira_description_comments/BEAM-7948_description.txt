Currently only size-based cache threshold is supported in data service. It should also support the time-based cache threshold. This is very important, especially for streaming jobs which are sensitive to the delay.