TestObserverNode combines unit tests for ObserverNode. The tests are of different types. I propose to split them into separate modules, factor out common methods, and optimize it so that it starts and shuts down MIniHDFSCluster once for the entire test rather than for individual test cases.