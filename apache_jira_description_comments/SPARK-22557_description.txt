ScalaTest 3.0 uses an implicit `Signaler`. This PR makes it sure all Spark tests uses `ThreadSignaler` explicitly which has the same default behavior of interrupting a thread on the JVM in ScalaTest 2.2.x. This will reduce potential flakiness inside the other test suites.