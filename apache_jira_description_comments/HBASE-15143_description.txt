We can query MasterProcedureScheduler to display the various procedures and who is holding table/region locks.

Each procedure is in a TableQueue or ServerQueue, so it is easy to display the procedures in its own group.