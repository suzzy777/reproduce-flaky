Currently support for more than 2 NameNodes (HDFS-6440) is only in branch-3. This JIRA aims to backport it to branch-2, as this is required by HDFS-12943 (consistent read from standby) backport to branch-2.