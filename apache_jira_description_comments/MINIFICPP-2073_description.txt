Since the docker tests are not as consistent as we would like, it would be nice if we could rerun just the tests without the need to rebuild the image.

We could use docker save/load image and github upload/download artifact to achive this capability.