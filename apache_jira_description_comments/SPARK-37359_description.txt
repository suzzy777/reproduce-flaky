For decom: We should look for "Decommission executors" instead of "Remove reason statistics:", the listenerbus doesn't have strong guarantees and depending on it makes this test flaky.

 

For R : It has not passed in weeks, lets disable it.