Current TestActivityStream is pretty flaky, it uses a huge regex to match the streams gadget src url. If any of the parameters in the url changes, or if the locale of the build agent is different, it would fail.