Hey...

Sometimes the piplines fail with not reason.  and just reruning them fixes the issue...

could you allow setting a max auto retry, to just auto retry the pipeline if it fails.

Preferably per task (line) retry.